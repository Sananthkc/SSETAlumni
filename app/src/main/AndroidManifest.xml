<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pro.ssetalumni">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".newjobs"></activity>
        <activity android:name=".EventEdit" />
        <activity android:name=".EventHomeScreen" />
        <activity android:name=".EventPostNew" />
        <activity android:name=".NewPost" />
        <activity android:name=".Home" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Login" />
        <activity android:name=".Dashboard" />
        <activity android:name=".SSET_reg" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>